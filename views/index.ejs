<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>ToDo App</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet' type='text/css' media='screen' href='/style.css'>
</head>
<body>
    <div class="container">
        <h2>ToDo App</h2>
        <form action="/addtask" method="POST">
            <input class="input-task" type="text" name="newtask" placeholder="Enter New Task">
            <button>Add</button>
            <h2>Tasks</h2>

            <% for( var i = 0; i < tasks.length; i++){ %>
                <li><input class="checkbox" type="checkbox" name="check" value="<%= tasks[i] %>" /> <%= tasks[i] %> </li>
            <% } %>

            <button formaction="/deletetask" type="submit">Delete</button>
        </form>
        <h2>Completed tasks</h2>
        <% for(let i = 0; i < completed.length; i++){ %>
            <li><input class="checkbox" type="checkbox" checked><%= completed[i] %> </li>
        <% } %>
    </div>
</body>
</html>